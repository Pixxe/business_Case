<div class="card col-lg-4 col-md-6">
    <img src="..." class="card-img-top" alt="...">
    <div class="card-body">
        <div class="text-center">
            <h5 class="card-title">{{ product.label }}</h5>
            <p class="card-text">{{ product.description }}</p>
            <a href="#" class="btn btn-primary">Voir le détails du produit</a>
            <a class="btn btn-green" href="{{ path('app_basket_add',{'id': product.id })}}">Ajouter au panier</a>
        </div>
    </div>
</div>